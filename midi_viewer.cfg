{
	'panels': [],

	# 'volume_formatter': (lambda note:
	#	'{}@{}→{}'.format(
	#		note.velocity,
	#		note.volume,
	#		note.volume_merge
	#	)),

	# LINEAR

	# 'volume_func': (lambda vol, vel: vol),
	# 'volume_formatter': (lambda note:
	#	'{}->{}@{}'.format(
	#		note.velocity,
	#		round((note.velocity/127) # ** 0.9
	#			* 15, 1),
	#		note.volume_merge
	#	)),
	# 'vbend_formatter': (lambda note: -note.volume_merge),

	# QUADRATIC
	# 'volume_func': (lambda vol, vel: (vol/127)**2 * (vel/127)**2 * 1000),

	# DOUBLE
	# 'volume_func': (lambda vol, vel: round(vol*vel/127*2)),

	'vol_expr': lambda v: 2*v,
}
